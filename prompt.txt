Perfecto, mira, ahora estoy viendo otro problema que tengo, ya que integré correctamente la ejecución del algoritmo en la interfaz ahora quiero abordar un problema con la visualización de los archivos creados, la cosa es que, cambié el wrapped para que me tirara los outputs de la ejecución el algoritmo a una carpeta llamada outputs en ka raiz del proyecto, de esta forma tendré mejor control sobre esta, lo que pasa es que, el visualizador toma los archivos que se cargan directamente, porque antes se cargaba directamtne el output del algoritmo, o sea el .vtk, ahora cargo el .poly y ejecuto el algoritmo, el tema es que necesito buscar una forma en la que, cuando yo cargue un archivo y se ejecute el algoritmo, se cree una carpeta dentro de la carpeta output con nombre de algún identificador del archivo .poly cargado, de esta forma el visualizador cuando yo seleccione algún archivo .poly en la lista de archivos cargador en la interfaz, el visualizador seleccione la carpeta correspondiente a los outputs que le correspondan al archivo seleccionado, necesito que me ayudes a realizar esta tarea, 